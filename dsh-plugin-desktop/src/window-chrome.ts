/** Compact visual inset reserved above enhanced-mode content on macOS. */
export const MACOS_TITLEBAR_HEIGHT = 20

/** Height of the macOS native drag hit regions in CSS pixels. */
export const MACOS_DRAG_REGION_HEIGHT = 36

/** Width reserved for the macOS traffic lights before the empty drag strip. */
export const MACOS_TRAFFIC_LIGHT_SAFE_WIDTH = 80

/** Windows title-bar overlay height in CSS pixels. */
export const WINDOWS_TITLEBAR_HEIGHT = 36

/** Width reserved for the three native Windows caption controls in CSS pixels. */
export const WINDOWS_CAPTION_CONTROLS_WIDTH = 138

/** Height of the independent Desktop frame used by compatibility and extended modes. */
export const DESKTOP_FRAME_HEIGHT = 36

/** Top edge that vertically centers the 12px macOS traffic lights in the frame. */
export const MACOS_TRAFFIC_LIGHT_TOP = 12

/** Radius of the content corner inside the inverted-L glass frame. */
export const EXTENDED_INNER_CORNER_RADIUS = 10
