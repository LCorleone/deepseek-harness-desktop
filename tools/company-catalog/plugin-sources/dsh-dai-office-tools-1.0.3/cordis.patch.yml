# dsh-dai-office-tools bundle patch: mount the host plugin under its package name,
# resolved through the profile's node_modules.

- insert:
    - id: dsh-dai-office-tools
      name: dsh-dai-office-tools
