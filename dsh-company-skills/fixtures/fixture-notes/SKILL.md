---
name: fixture-notes
description: Second fixture skill for the company-skills container and provider tests.
---

# Fixture notes

This fixture is the second skill of the shipped container so the provider
tests can assert that `list()` returns every bundled skill while `get()`
materializes exactly one body.

Read `assets/notes.md` for the placeholder note file.
