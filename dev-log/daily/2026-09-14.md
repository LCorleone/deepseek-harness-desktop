# 2026-09-14 工作记录

## 一句话

b93 fleet 迁移完成 → 目录三连发（stable 27→35）→ Lucy 案例挖出两条真 bug 并修复 → **b94 构建+发版+真机 3/4 验证通过**。

## 目录（MR session 协同 + 本 session）

- stable：seq27 → **seq35**（9 条，8 可装）：+dsh-company-skills 0.1.2（v2 brotli，16.7MB，方案 B：beta seq31 浸泡→批量转正 seq33）+dsh-dai-mobius 0.1.0/0.1.1 +dsh-dai-office-tools 1.0.3；−engramory（无钥 retire，beta seq34，July 拍板不要了）
- beta：seq34（10 条）。棘轮 35。repository 惯例终版：同事件一律 `plugin-market.s.dai.deloitte.cn/packages/<name>`（SOP/MR-HANDLING 已落，`a52a291d9e`）
- 发布路上踩雷一枚并修复：#011 后 `assets/` 目录 fresh-checkout 不存在 → 重建脚本 ENOENT（`bfef3b7661`，mkdirSync 两处写入点）

## 修复（全部进 b94，均过评审）

| # | 修的什么 | commit |
|---|---|---|
| #031 | 重启授权误用安装预览 5min TTL（sebtang 11 分钟点重启被拒实证）→ 独立 24h，含禁用/启用路径与幂等窗 | `8dafdae598` |
| #033 | 共享 Python venv 判据不含 pip → dsh-pip 永不发布且无自愈无遥测（Lucy「没有 dsh-pip 命令」）→ 判据含 pip + ensurepip 原地自愈 + python_runtime 加 shared/pip 字段 | `091840e423` |
| #034 | sandbox_escalation 遥测构建期双实例断裂（tsdown 内联 main.js vs loader 加载 lib 文件，July 弹窗实证 vs 全库 0 条）→ Symbol.for 跨实例槽 + 双实例红绿测试 + 兄弟审计（唯一断裂点） | `e9e6832a90` |
| #035 | 启动/重启 4-12s 死区无反馈 → 内容空白化静态 splash（零脚本/零 preload）、300ms 防闪、showInactive 不抢焦点、onFirstVisibleSurface 链退场；三轮评审修掉双建竞态（活引用幂等守卫）+ pre-ready isReady 门 | `0aa666f4bb`+`1f2f4c3464` |
| 发版闸 | 内嵌兜底清单防烂测试进 check:win-package（构建自带此闸，不再依赖 CI 传递绿） | `271284e0f2` |

## b94

- 构建 run 34845032063，HEAD `c3835b1ce2`（含上述全部+内嵌清单刷 seq35），tag `v2.0.4-b94` 双推
- 包：`tmp_sessions/dsh-desktop-asserts/DSH-Desktop-2.0.4-x64-Setup-b94.exe`，169,746,993 B，sha256 `5415957d1d9f7e7d31d6a9275f0c71debe0dda751087ac6698306c5929b3bafd`
- 发版前：六 commit 组合终审 APPROVED（RELEASE-READY）+ P3① 修+补评过
- **真机验证 3/4**：①转圈窗肉眼确认 ✓ ②python_runtime `{shared:true,pip:true}` 字段上线即工作 ✓ ③sandbox_escalation **全库首条**（approved）=#034+#033 端到端 ✓ ④装后晾 21 分钟点重启 accepted、intent-expired 归零 ✓。**剩：Lucy pip 自愈（明天）**

## 关键认知（记档的教训）

- **模块缝结论必须区分源码视图 vs 构建产物视图**：#007 当年「接线完好」只验了存在性没验实例同一性，双实例断裂存活多日
- agent 会「聪明变通」绕过事前闸（dsh-pip 缺失时自己改跑 `python -m pip` → 沙箱挂死无弹窗）——闸的覆盖面与 persona 措辞要跟着堵
- publish-local 网络抖动时 gh 下载会自动落到 catalog-artifacts 分支（今天实证一次）

## 明天

1. Lucy 升 b94 重启 → 查她 python_runtime 的 pip 字段（#033 自愈实证，#004 第 4 锚点）
2. b94 发 fleet（优先 lizywu/tammtang，b90 双痛点：装插件失败+无转圈）
3. ~09-16：#032（0.1.2 stable 泡干净后 retire 0.1.0/0.1.1 + 撤 v1 钉，工作树再 −114MB）

## Open（10）：#004（观察）/ #032（09-16）/ #005/#010/#006/#009/#022/#019/#020/#021
